{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reponse list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Reponse</th>
                <th>Justification</th>
                <th>Etat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reponse_show', { 'id': entity.id }) }}">{{ entity.reponse }}</a></td>
                <td>{{ entity.justification }}</td>
                <td>{{ entity.etat }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reponse_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reponse_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reponse_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
