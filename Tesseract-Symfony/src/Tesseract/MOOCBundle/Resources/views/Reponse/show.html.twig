{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reponse</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Reponse</th>
                <td>{{ entity.reponse }}</td>
            </tr>
            <tr>
                <th>Justification</th>
                <td>{{ entity.justification }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ entity.etat }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('reponse') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('reponse_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
