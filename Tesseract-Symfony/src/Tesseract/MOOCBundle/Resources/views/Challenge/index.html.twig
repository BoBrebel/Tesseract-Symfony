{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Challenge list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>Theme</th>
                <th>Date</th>
                <th>Idutilisateur</th>
                <th>Duree</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('challenge_show', { 'id': entity.id }) }}">{{ entity.nom }}</a></td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.theme }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.idUtilisateur }}</td>
                <td>{{ entity.duree }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('challenge_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('challenge_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('challenge_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
